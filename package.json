{
  "name": "fundsroom-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  }
}
